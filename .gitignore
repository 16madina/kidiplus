# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Local machine backups / duplicates (not the real ios/ or lockfile)
ios.local-backup/
package-lock.json.local-backup
# Firebase plist already lives at ios/App/App/GoogleService-Info.plist
/GoogleService-Info.plist

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Huge Lovable demo videos mirrored for localhost (not for git)
public/__l5e/**/*.mov
public/__l5e/**/demo-video.mp4
.cutover-keep/

# Local DB dumps / migration artifacts (never commit)
.env.migration
migration-data/*.backup
migration-data/backup-*/
migration-data/*.saved.sql
migration-data/storage-report/
